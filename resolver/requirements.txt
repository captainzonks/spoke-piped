# Pinned for reproducible builds. NOTE: yt-dlp and the bgutil plugin must be
# bumped regularly — YouTube changes break extraction; treat these as the
# fastest-moving pins in the project. The bgutil plugin version SHOULD match
# the piped-bgutil provider server image tag.
fastapi==0.136.3
uvicorn[standard]==0.49.0
yt-dlp==2026.8.19
bgutil-ytdlp-pot-provider==1.3.1
cachetools==7.1.4
blake3==1.0.8
